# UpscalyVid runtime dependencies
# torch and numpy are expected to already be installed in the environment.
# Install the extras below if they are missing:
#
#   pip install -r requirements.txt
#
# For GPU support make sure to install the CUDA-enabled torch wheel first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128

torch>=2.0.0
numpy>=1.24.0
Pillow>=9.0.0
